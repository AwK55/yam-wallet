'use strict';

const main = require('./main');
const routes = require('./routes');
const koa = require('./koa');
const log = require('./logger');
module.exports = {
    main,
    routes,
    log
    //koaConfig
}
