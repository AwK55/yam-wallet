'use strict';

const _ = require('lodash');

const main = require('./main');
const routes = require('./routes');
const koaConfig = require('./koa');

module.exports = {
    main,
    routes
    //koaConfig
}
